# Деплой Telegram-бота на Bult.ai

## Что нужно подготовить

| Параметр | Где взять |
|----------|-----------|
| **BOT_TOKEN** | @BotFather в Telegram -> `/newbot` -> скопировать токен |
| **ADMIN_ID** | @userinfobot в Telegram -> `/start` -> скопировать Id |
| **GOOGLE_SHEET_ID** | Создать таблицу на sheets.google.com, из URL скопировать часть между `/d/` и `/edit` |
| **CHANNEL_LINK** | Ссылка на ваш Telegram-канал (опционально) |

---

## 1. Деплой на Bult.ai

1. Зарегистрируйтесь на [bult.ai](https://bult.ai)
2. Создайте новый проект -> подключите GitHub-репозиторий `dynyaa/TgBotAsl`
3. Добавьте **переменные окружения** (Environment Variables):

| Переменная | Значение |
|------------|----------|
| `BOT_TOKEN` | Токен от BotFather |
| `ADMIN_ID` | Ваш Telegram ID |
| `GOOGLE_SHEET_ID` | ID Google-таблицы |
| `CHANNEL_LINK` | Ссылка на канал (или оставить пустым) |
| `GOOGLE_CREDENTIALS_PATH` | `credentials.json` |

4. Нажмите **Deploy**

Bult автоматически определит Python-проект, установит зависимости и запустит бота.

---

## 2. (Опционально) Google Sheets API

Без этого бот работает, но не экспортирует заявки в таблицу.

1. [console.cloud.google.com](https://console.cloud.google.com) -> **API и сервисы** -> **Библиотека**
2. Включить **Google Sheets API** и **Google Drive API**
3. **Учетные данные** -> **Создать** -> **Сервисный аккаунт** -> назвать -> **Создать**
4. Открыть аккаунт -> **Ключи** -> **Добавить ключ** -> **JSON** -> скачается `credentials.json`
5. Из `credentials.json` скопировать `client_email`
6. В Google Таблице -> **Настройки доступа** -> вставить email -> **Редактор** -> **Отправить**
7. Загрузить `credentials.json` в корень проекта на GitHub (или через Bult volumes)

---

## 3. Управление ботом

### Через Telegram

Отправьте `/admin` боту:
- **Вебинары** -> **Добавить вебинар** (название, дата ДД.ММ.ГГГГ, время ЧЧ:ММ, часовой пояс, ссылка)
- **Программы** -> добавить/редактировать программы
- **Настройки** -> контакт менеджера, ссылка на канал, материалы

### Обновление кода

Просто запушьте изменения в GitHub — Bult автоматически передеплоит.

---

## Проблемы и решения

| Проблема | Решение |
|----------|---------|
| Бот не отвечает | Проверить логи в Bult dashboard, убедиться что BOT_TOKEN верный |
| Не пишет в Google Sheets | Проверить `credentials.json` и доступ к таблице (раздел 2) |
| Deploy failed | Проверить логи сборки в Bult, убедиться что `requirements.txt` корректный |

---

**Репозиторий:** https://github.com/dynyaa/TgBotAsl
